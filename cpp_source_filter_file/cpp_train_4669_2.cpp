#include<bits/stdc++.h>
using namespace std;
int main(){
  int n;cin>>n;
  vector<pair<int,int>>a(n);for(int i=0;i<n;i++)cin>>s.at(i).first>>s.at(i).second;
  sort(a.begin(),a.end());
  cout<<a.at(n-1).first+a.at(n-1).second<<endl;
}