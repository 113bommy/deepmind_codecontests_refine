#include <bits/stdc++.h>
using namespace std;
const long long INF = (1LL << 61) - 1;
const long long MOD = 1000000007;
const long long MAX_N = 500010;
long long a, b, c, d, e, f, h, x, y, z, p, q, n, t, r, k, w, l, ans, i, j, u, v,
    m;
long long codeforces = 1;
string S, T;
vector<long long> A, B;
void input() { cin >> codeforces; }
void solve() {
  cin >> n;
  cout << (n - 1) / 2 << "\n";
}
int main() {
  cin.tie(0);
  ios::sync_with_stdio(false);
  input();
  while (codeforces--) {
    solve();
  }
}
