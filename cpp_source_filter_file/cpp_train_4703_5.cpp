#include <bits/stdc++.h>
using namespace std;
const int N = 50001;
int r[N], c[N];
int main() {
  ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
  int t;
  cin >> t;
  while (t--) {
    int x, n;
    cin >> x >> n;
    cout << (n << 1);
  }
  return 0;
}
