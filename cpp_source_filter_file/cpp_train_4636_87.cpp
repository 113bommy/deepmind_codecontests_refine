#include <bits/stdc++.h>
using namespace std;
int main() {
  map<string, int> x;
  string s;
  long long n, i, q, sum = 0, z = 1;
  cin >> n;
  if (n == 1)
    cout << 1 << endl;
  else
    cout << 2 << endl;
  return 0;
}
