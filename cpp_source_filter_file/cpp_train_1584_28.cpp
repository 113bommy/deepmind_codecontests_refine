#include <bits/stdc++.h>
using namespace std;
int main() {
  ios_base::sync_with_stdio(false), cin.tie(0), cout.tie(0);
  srand(time(NULL));
  ;
  long long t, n, m;
  t = 1;
  while (t--) {
    cin >> n;
    cout << n * 6 << " " << n * 5;
  }
  return 0;
}
