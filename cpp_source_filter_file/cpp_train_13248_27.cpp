#include <bits/stdc++.h>
using namespace std;
int main() {
  ios::sync_with_stdio(0);
  cin.tie(0);
  cout.tie(0);
  ;
  long long t, b, i, n;
  cin >> t;
  while (t--) {
    cin >> n;
    cout << (n * n) / (n + 1) << endl;
  }
  return 0;
}
