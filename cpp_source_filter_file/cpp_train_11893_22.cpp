#include <bits/stdc++.h>
#pragma GCC optimize("O3")
#pragma GCC target("avx")
#pragma GCC optimize("Ofast")
#pragma GCC optimize("unroll-loops")
#pragma GCC target("sse,sse2,sse3,ssse3,sse4,popcnt,abm,mmx,avx,tune=native")
int64_t n, m, d, t, i, j, k, l, r, x, y, z, cnt, sum = 0, mid, mx = -INFINITY,
                                                 mn = 1000000;
bool f, f1, f2;
using namespace std;
int main() {
  ios_base::sync_with_stdio(0);
  cin.tie(0);
  cout.tie(0);
  cin >> x;
  cout << "2 3";
  cout << "\n";
  cout << +x << " " << 131072 << " " << 0;
  cout << "\n";
  cout << x << " " << 131072 + x << " " << x;
  cout << "\n";
}
