#include<stdio.h>
#include<algorithm>
int*it,shop[100001],n,m,d;
main(i){
	for(;scanf("%d%d%d",&d,&n,&m),shop[i=n]=d;i=!printf("%d\n",i)){
		for(;--i;)
			scanf("%d",shop+i);
		for(std::sort(shop,shop+n);m--;i+=std::min(*it - d, d - it[-1] ))
			scanf("%d",&d),
			it = std::upper_bound( shop, shop+n, d );
	}
}