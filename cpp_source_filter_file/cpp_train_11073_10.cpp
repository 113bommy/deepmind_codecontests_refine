#include <bits/stdc++.h>
using namespace std;
unsigned long long n = 0, m = 100, i, j, k, ans = -1, y, x, z, l, a, b, c, s,
                   o = 0;
bool can = false;
int main() {
  std::ios_base::sync_with_stdio(false);
  cin >> n;
  if (n < 3)
    cout << -1;
  else
    while (n--) cout << n + 1;
  return 0;
}
