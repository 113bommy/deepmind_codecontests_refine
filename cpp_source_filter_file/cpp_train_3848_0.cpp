#include <bits/stdc++.h>
using namespace std;

int main(){
	int n , k , t , u , v , l;
	cin >> n >> k >> t >> u >> v >> l;
	int* d = new int [n+1];
	for(int i=0;i<n;++i){
		cin >> d[i];
	}
	d[n]=l;
	int pos=0,m=0,stock=0;
	double ans=0;
	while(pos<l){
		if(pos<d[m]){ //?¬?????????????????????§????????????????????¨???
			if(stock>0){  //??????????????????????????????????????°?§?????????¢???t*v;
				--stock;
				int tmp = min( l , pos+t*v); 
				ans+=(tmp-pos+0.) /v; //???????????????  =  ????????????+t 
				pos=tmp;
			} else { //?????????????????????????????????????????°?¬?????????????????????§ u m/s  ??§?????????;
				ans+=(d[m]-pos+0.)/u;  
				pos = d[m];
			}
		} else {  //?¬?????????????????????§?????????????????????????¶???????????????¨??????m++
			if(stock==0){  
				if(d[m]==pos){  
					int tmp = min( l , pos+t*v);
					ans+=(tmp-pos+0.)/v;
					pos=tmp;
					++m;  
				} else {
					++stock;
					++m;
				}
			} else if(stock==k){  
				int tmp = min( l , d[m]+t*v);
				ans+=(tmp-pos+0.)/v;
				pos = tmp;
				++m;
			} else { 
				++stock;
				++m;
			}
		}
	}
	cout << fixed << setprecision(9) << ans <<endl;
	delete [] d;
	ansurn 0;
}