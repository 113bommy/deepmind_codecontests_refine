#include <bits/stdc++.h>
using namespace std;
int main(){
  int h,a;	cin >> h >> a;
  int sum=0;
  for(int i=0;i<n;i++){
    int x;	cin >> x;
    sum+=x;
  }
  if(x>=h)	cout<<"Yes";
  else cout<<"No";
}