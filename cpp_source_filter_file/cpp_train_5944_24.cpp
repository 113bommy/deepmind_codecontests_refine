#include <bits/stdc++.h>
using namespace std;
int main() {
  long long n, i, j, s, ans, p, r, t, q, m, x, y, x1, y1, l;
  cin >> t;
  while (t--) {
    cin >> x;
    cout << x + (x % 2) << " ";
  }
}
