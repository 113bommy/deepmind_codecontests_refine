#include <bits/stdc++.h>
using namespace std;
const long long inf = 1e9 + 7;
signed main() {
  cin.tie(0);
  cout.tie(0);
  ios_base::sync_with_stdio(0);
  long long t;
  cin >> t;
  while (t--) {
    long long a, b, c, d, x, y, z;
    cin >> a >> b >> c >> d;
    cout << b << " " << b << " " << c << endl;
  }
}
