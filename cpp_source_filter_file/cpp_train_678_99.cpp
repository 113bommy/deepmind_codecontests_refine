#include <bits/stdc++.h>
using namespace std;
string s1;
int num, day, d[205], v[205], maxi, flag, low, up, mid, cnt, n1, n2, mini, tmp,
    nxt, s, sum, row, tar, x[205], c[205][205], vote;
vector<int> Q[205];
int main() {
  scanf("%d", &num);
  printf("%d\n", (sum + 1) / 2);
  return 0;
}
