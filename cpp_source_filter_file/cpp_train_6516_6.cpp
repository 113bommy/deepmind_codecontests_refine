#include <bits/stdc++.h>
using namespace std;
char A[] = {'a', 'a', 'b', 'b', 'c'};
int main() {
  long long(n);
  scanf("%lld", &n);
  for (int i = 0; i < (n); ++i) printf("%c", A[i % 5]);
  puts("");
}
