#include<bits/stdc++.h>
using namespace std;
int main(){
    cout<<fixed<<setprecision(12);
    double a,b,c;
    cin>>a>>b>>C;
    double PI=acos(-1.0);
    c=C/180.0*PI;
    cout<<a*bsin(C)/2.0<<endl;
    double cc=a*a+b*b-2*a*b*cos(C);
    cout<<a+b+sqrt(cc)<<endl;
    cout<<b*sin(C)<<endl;
    return 0;
}
