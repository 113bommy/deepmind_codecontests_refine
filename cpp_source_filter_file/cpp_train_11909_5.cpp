#include <bits/stdc++.h>
using namespace std;
int main() {
  long long a, b, c, i, j, k, l, n, m, d, q, s;
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);
  cin >> n;
  if (n % 2 == 0)
    cout << -1;
  else {
    for (i = 0; i < n; i++) cout << i << ' ';
    cout << endl;
    for (i = 0; i < n; i++) cout << i << ' ';
    cout << endl;
    for (i = 0; i < n; i++) cout << i << ' ';
    cout << endl;
  }
}
