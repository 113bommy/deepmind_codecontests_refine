#include <bits/stdc++.h>
using namespace std;
const long long int N = 1e6 + 10;
const long long int inf = 1e18;
const double pi = 3.1415926536;
long long int n, i, a, b, c, j, k, y, x, t, l, r, d, cnt, cnt1, cnt2, p, e, f,
    m, flag = 1, sum, h, w = 1, mi = inf, ma = -inf;
vector<long long int> v[N];
stack<long long int> st;
bool vis1[N], vis[N];
priority_queue<long long int> q;
map<long long int, long long int> mp;
deque<long long int> l2;
int main() {
  cin >> a >> b;
  cout << (w * w / 4) * (h * h / 4);
}
