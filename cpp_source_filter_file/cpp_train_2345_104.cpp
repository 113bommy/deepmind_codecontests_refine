#include <bits/stdc++.h>
using namespace std;
const long long N = 1e4 + 1;
const long long INF = 1e18;
long long n, m, l, k, x, y, z, cnt, ans, sum, mx, mn = INF;
vector<long long> v;
deque<long long> d;
string s, t;
map<long long, long long> mp;
int main() {
  ios_base::sync_with_stdio(0);
  ;
  cin >> n;
  cout << n / 2500;
}
