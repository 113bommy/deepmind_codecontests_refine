#include <bits/stdc++.h>
using namespace std;
int main() {
  long long t = 1;
  while (t--) {
    vector<long long> w;
    long long n, k;
    cin >> n >> k;
    cout << (n / k + 1) * k;
    cout << "\n";
  }
}
