
#include<iostream>
#include<cstdio>
#include<cstdlib>
#include<algorithm>
#include<stack>
#include<cstring>
#include<string>
#include<sstream>
using namespace std;

int main()
{
	string s;
	while (getline(cin, s)) {
		if (cin.eof())break;//if(s=="")break;
		stack<double> st;
		stringstream ss(s);
		string tmp;
		while (ss >> tmp) {
			if (tmp == "+" || tmp == "-" || tmp == "*" || tmp == "/") {//???????¨?????????\????????£??????
				if (st.empty())break;//?????????????????????????????£????????????
				double z1 = st.top();
				st.pop();//stack?????????????????????
				double z2 = st.top();
				st.pop();//stack?????????????????????
				double z3 = 0.0;
				if (tmp == "+")z3 = z2 + z1;
				else if (tmp == "-")z3 = z2 - z1;
				else if (tmp == "*")z3 = z2 * z1;
				else if (tmp == "/")z3 = z2 / z1;
				st.push(z3);
			}
			else {//??°????????\????????£??????
				st.push(atof(tmp.c_str()));//double?????????????????????????????\??????
			}
		}
		printf("%.6f\n", st.top());
	}
    return 0;
}