#include <bits/stdc++.h>
using namespace std;
int main() {
  {
    long long int n, c, m, i, j, count = 0, x, a = 1, b, y, sum = 0;
    long long int mn = 1e18, mx = 0, ans = 0, k, ind;
    string s;
    cin >> n >> x >> y;
    if ((n == 2 * x or n == (2 * x) - 2) and (n == 2 * y or n == (2 * y) - 2))
      printf("NO\n");
    else
      printf("YES\n");
  }
  return 0;
}
