#include <bits/stdc++.h>
using namespace std;
int main() {
  ios_base::sync_with_stdio(0);
  cin.tie(NULL);
  long long tt;
  for (cin >> tt; tt; --tt) {
    long long n;
    cin >> n;
    for (int i = 1; i <= n; i++) cout << 1 << " ";
    cout << endl;
  }
}
