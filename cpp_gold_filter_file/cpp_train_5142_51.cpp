#include <bits/stdc++.h>
using namespace std;
int main() {
  for (int i = 0, ThxDem = 10; i < ThxDem; ++i) printf("??%d>>%d??\n", i, i);
  puts("\?\?>>?");
  for (int i = 0, ThxDem = 9; i < ThxDem; ++i) printf("%d?<>%d\n", i, i + 1);
  puts("9?>>?0\n?<>1");
  for (int i = 0, ThxDem = 10; i < ThxDem; ++i) printf("%d>>%d??\n", i, i);
  return 0;
}
