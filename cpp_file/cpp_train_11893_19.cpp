#include <bits/stdc++.h>
using namespace std;
int main() {
  int timer;
  timer = 1;
  while (timer--) {
    long long n, a, b, c;
    a = pow(2, 17);
    b = a;
    c = a;
    cin >> n;
    cout << 2 << " " << 3 << "\n";
    cout << (a + n) << " " << a << " " << 0 << "\n";
    cout << n << " " << (a + n) << " " << n << "\n";
  }
}
