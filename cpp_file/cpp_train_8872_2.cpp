#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using pi = pair<int, int>;
using vi = vector<int>;
using vvi = vector<vi>;
void solve(int tt) {
  int n;
  cin >> n;
  cout << n << endl;
}
bool boosts = false, tests = true;
int main() {
  if (boosts) {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
  }
  int tcases = 1;
  if (tests) {
    cin >> tcases;
  }
  for (int tcase = 1; tcase <= tcases; tcase++) {
    solve(tcase);
  }
  return 0;
}
