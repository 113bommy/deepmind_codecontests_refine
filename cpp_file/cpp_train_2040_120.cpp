#include <bits/stdc++.h>
using namespace std;
bool test = 0;
void solve() {
  int n;
  cin >> n;
  cout << (n - 1) / 2;
}
int main() {
  ios::sync_with_stdio(0);
  cout.tie(0);
  cin.tie(0);
  if (test) {
    int t;
    cin >> t;
    while (t--) solve();
  } else
    solve();
  return 0;
}
