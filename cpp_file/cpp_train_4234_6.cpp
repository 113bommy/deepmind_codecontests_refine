#include <bits/stdc++.h>
using namespace std;
int main() {
  int r, b;
  scanf("%d%d", &r, &b);
  for (int i = 0; i < r + b; i++) {
    long long x, y;
    cin >> x >> y;
  }
  if (b == r) {
    cout << "Yes";
  } else {
    cout << "No";
  }
  return 0;
}
