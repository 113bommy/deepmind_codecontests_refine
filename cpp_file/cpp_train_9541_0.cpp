#include <bits/stdc++.h>
const int maxn = 1e3 + 5;
int n;
char str[maxn];
int main() {
  std::cin >> str + 1;
  n = strlen(str + 1);
  std::cout << str + 1;
  for (int i = n - 1; i; i--) putchar(str[i]);
  return 0;
}
