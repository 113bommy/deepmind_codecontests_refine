#include <bits/stdc++.h>
using namespace std;
int n, m, ans;
int main() {
  ios::sync_with_stdio(false), cout.tie(NULL), cin.tie(NULL);
  ;
  cin >> n >> m;
  ans = n + m;
  cout << ans << endl;
  return 0;
}
