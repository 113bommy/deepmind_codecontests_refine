#include <bits/stdc++.h>
using namespace std;
signed main() {
  int a, b, r;
  cin >> a >> b >> r;
  if (2 * r <= a and 2 * r <= b)
    cout << "First" << '\n';
  else
    cout << "Second" << '\n';
}
