#include <bits/stdc++.h>
using namespace std;
vector<int> v, q;
long long int n, m, cnt, ans, arr[10003], ram, p, a, b, c, d, e;
float fl, tl, sl, hl;
double gd, fd, sd;
long double k, l, i;
string s, t;
set<int> y;
vector<string> h;
pair<long long int, long long int> P;
map<long long int, long long int> mp;
set<long long int, long long int> St;
int main() {
  cin >> m;
  for (int i = 1; i <= m; i++) {
    cin >> ans >> ram;
    cout << ans << " " << ans * 2 << endl;
  }
}
