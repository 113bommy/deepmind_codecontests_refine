#include <bits/stdc++.h>
using namespace std;
int main() {
  ios ::sync_with_stdio(false);
  cin.tie(0);
  ;
  long long int n, a, i, j, z, c = 0;
  string s, p;
  cin >> s >> p;
  if (s == p)
    cout << p;
  else
    cout << 1;
  return 0;
}
